import React from 'react';

/**
 * This icon component is no longer used in the application.
 * Video controls are now native to the browser's video element.
 */
const PlayIcon: React.FC<{ className?: string }> = () => null;

export default PlayIcon;